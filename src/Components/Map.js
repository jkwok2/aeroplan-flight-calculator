import React, {Component} from 'react'
import TextField from '@material-ui/core/TextField';

class Map extends Component {
  

    render(){
        return (  // every component needs a render
            <TextField id="outlined-basic" label="Map" variant="outlined" />
        )
    }
}

export default Map;
