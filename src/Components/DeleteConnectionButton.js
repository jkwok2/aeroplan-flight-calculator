import React, {Component} from 'react'

class DeleteConnectionButton extends Component {
  

    render(){
        return (  // every component needs a render
           
        )
    }
}

export default DeleteConnectionButton;
