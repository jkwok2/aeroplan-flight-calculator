import React, {Component} from 'react'

class AddConnectionButton extends Component {
  

    render(){
        return (  // every component needs a render
           
        )
    }
}

export default AddConnectionButton;
